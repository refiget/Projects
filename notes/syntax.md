Principle: 只需要记录反复忘记的语法

# nvim

## Replace in the whole file
```
:%s/\<promt\>prompt/g

```


# Python

## 20251126
1. Key is the direct element of dict. For a Value, we can use *******dict.value* to get the value list.
2. Return statement interrupts any loop and immediately terminates the function, returning the specified value. 
3. "Append" element to list, set, and dict:
    a. list: append
    b. set: add
    c. dict: update or dict[index] = value
4. Iterate in range(2, 2) immediately terminates the loop. If the iterator is empty, the loop terminates immediately. 
5. Str variable is iterable.
